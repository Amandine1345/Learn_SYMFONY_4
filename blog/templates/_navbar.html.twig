<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5">
    <a class="navbar-brand" href="{{ path('app_index') }}">{{ 'title'|trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav">
            <li><a class="nav-item nav-link" href="{{ path('blog_index') }}">{{ 'article.list'|trans }}</a></li>
        </ul>
        <ul class="navbar-nav border-left">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        {{ 'admin.title'|trans }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('article_index') }}">{{ 'nav.item1'|trans }}</a>
                        <a class="dropdown-item" href="{{ path('category_index') }}">{{ 'nav.item2'|trans }}</a>
                        <a class="dropdown-item" href="{{ path('tag_index') }}">{{ 'nav.item3'|trans }}</a>
                    </div>
                </li>

                <li>
                    <a class="nav-item nav-link" href="{{ path('app_logout') }}">{{ 'account.logout'|trans }}</a>
                </li>
            {% else %}
                <li>
                    <a class="nav-item nav-link" href="{{ path('app_login') }}">{{ 'account.login'|trans }}</a>
                </li>
            {% endif %}

            <li class="nav-item dropdown border-left">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    {{ 'choose.language'|trans }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{ path('app_index.fr', {'_locale': 'fr'}) }}">Français</a>
                    <a class="dropdown-item" href="{{ path('app_index.en', {'_locale':'en'}) }}">English</a>
                    <a class="dropdown-item" href="{{ path('app_index.es', {'_locale': 'es'}) }}">Español</a>
                </div>
            </li>
        </ul>
    </div>
</nav>